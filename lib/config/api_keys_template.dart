// Copyright (c) 2026 NLP digitox
//
// TEMPLATE — copy this file to `lib/config/api_keys.dart` and paste your keys.
//
//   cp lib/config/api_keys_template.dart lib/config/api_keys.dart
//
// `api_keys.dart` is GITIGNORED (see .gitignore) so your real keys are never
// pushed to GitHub. This template IS committed so fresh clones know the shape.
//
// Get a free Groq key at https://console.groq.com/keys
// Get a Gemini key at https://aistudio.google.com/apikey

class ApiKeys {
  /// Groq API key used by the chatbot + sentiment analysis services.
  static const String groqApiKey = '';

  /// Gemini API key (optional, reserved for future use).
  static const String geminiApiKey = '';
}
